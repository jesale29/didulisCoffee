<nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm">
    <div class="container">
        <a class="navbar-brand fw-bold text-warning" href="<%= route('public.home') %>">
            <i class="bi bi-cup-hot-fill me-2"></i>Didulis Coffee
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link" href="<%= route('public.home') %>">Home</a>
                </a>
            </li>
            </ul>

        <ul class="navbar-nav ms-auto">
            <% if (user) { %>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown">
                        <i class="bi bi-person-circle me-2 fs-5"></i>
                        <%= user.name %>
                    </a>
                    <ul class="dropdown-menu dropdown-menu-end shadow border-0">
                        <li><h6 class="dropdown-header text-uppercase small text-muted">Account</h6></li>
                        <li><a class="dropdown-item" href="#">My Profile</a></li>
                        <li><a class="dropdown-item" href="#">Order History</a></li>

                        <% if (user.role === 'admin' || user.role === 'staff') { %>
                            <li><hr class="dropdown-divider"></li>
                            <li><h6 class="dropdown-header text-uppercase small text-warning">Management</h6></li>
                            <li>
                                <a class="dropdown-item fw-bold" href="<%= route('admin.dashboard') %>">
                                    <i class="bi bi-speedometer2 me-2"></i>Admin Hub
                                </a>
                            </li>
                            <li>
                                <a class="dropdown-item text-secondary" href="<%= route('admin.inventory.index') %>">
                                    <i class="bi bi-box-seam me-2"></i>Inventory Control
                                </a>
                            </li>
                        <% } %>
                        <li><hr class="dropdown-divider"></li>
                        <li>
                            <a class="dropdown-item text-danger" href="<%= route('auth.logout') %>">
                                <i class="bi bi-box-arrow-right me-2"></i>Logout
                            </a>
                        </li>
                    </ul>
                </li>
            <% } else { %>
                <li class="nav-item">
                    <a class="nav-link btn btn-outline-warning text-warning px-3" href="<%= route('auth.login') %>">Login</a>
                </li>
            <% } %>
        </ul>
    </div>
</div>
</nav>